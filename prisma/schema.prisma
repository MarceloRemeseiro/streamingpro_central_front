generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model ProcessState {
  id            String   @id
  isEnabled     Boolean  @default(false)
  lastUpdated   DateTime @default(now())
} 